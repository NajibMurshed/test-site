* git init . (initialize empty git repository)
1. git add . (this stages all untracked files)
2. git commit -m "message" (commits all changes)
3. git push origin master (push changes to remote master branch)
4. git checkout -b branch-name (checkout out to a new branch)
5. git checkout branch-name (switches to an existing branch)
